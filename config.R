# config
dataset_names <- c(
    "gajewski",
    "pittsburgh",
    "wargo",
    "zitvogel"
)
norm_methods <- c(

)
feat_types <- c(
    "cdd",
    "ec",
    "go",
    "lkt",
    "pfam",
    "prints"
)
filter_types <- c(
    "none",
    "filtered"
)
merge_types <- c(
    "none",
    "merged"
)
bc_methods <- c(
    "none",
    "ctr",
    "std",
    "rta",
    "rtg",
    "qnorm",
    # "cbt",
    # "fab",
    # "sva",
    "stica0",
    "stica025",
    "stica05",
    "stica1",
    "svd"
)
matfact_k <- 20
